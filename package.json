{
  "name": "mmo",
  "private": false,
  "version": "1.0.0",
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "colyseus": "^0.15.57",
    "react": "^19.0.0",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "build:client": "cd packages/client && bun run build",
    "build:server": "cd packages/server && bun run build"
  }
}
